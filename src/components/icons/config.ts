import React from 'react';
export interface IconProps {
  color?: React.CSSProperties['color'];
  className?: string;
}
import RoundCloseIcon from './round-close-icon';
export default {
  roundClose: RoundCloseIcon,
};
